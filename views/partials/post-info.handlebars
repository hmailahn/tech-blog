<article class="post">
    <div class="meta">
        <a href="/post/{{id}}">
        <div class="title">
    <h2>{{title}}</h2>
    </div></a>
    <h4>By: {{user.username}} Created at {{format_date created_at}}</h4>
    <p>{{body}}</p>
  </div>
  <div class="meta">
    <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
  </div>
</article>

